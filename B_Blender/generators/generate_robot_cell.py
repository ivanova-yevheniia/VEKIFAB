"""
generate_robot_cell.py — VEKIFAB WP B station generator.

Placeholder. Generates a robotic work CELL (arm + safety fence) in Blender.

No geometry is generated yet. When implemented, this script will build a single
robot cell and name its objects per docs/unity_import_notes.md
(STATION_*, PHYS_*, COLLIDER_*, TRIGGER_*, INFO_*), using helpers from
common_blender_utils.py.
"""

# import common_blender_utils as utils


def generate(station: dict):
    """Build a robot cell from its factory_description entry. Placeholder."""
    raise NotImplementedError


if __name__ == "__main__":
    print("generate_robot_cell.py — placeholder, no geometry generated yet.")
